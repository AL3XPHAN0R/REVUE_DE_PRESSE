# Secrets
.env

# Draft contains personal account details (Reddit username, repo link)
_config/reddit_api_application_draft.md

# Python
__pycache__/
*.pyc
*.pyo

# Stage outputs (transient — regenerated each run)
stages/01_fetch/output/*.json
stages/02_curate/output/*.md

# Stage 03 archives are kept (versioned digest history)
# stages/03_send/output/ is tracked
